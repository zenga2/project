{
  "name": "stylus-demo",
  "version": "1.0.0",
  "description": "stylus demo",
  "main": "index.js",
  "scripts": {
    "build": "stylus -w src/stylus/  -o dist/",
    "help": "stylus -i",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "zeng",
  "license": "ISC",
  "devDependencies": {
    "stylus": "^0.54.5"
  }
}
